<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
            xmlns:controls="clr-namespace:Timeline.Controls"
            x:Class="Timeline.Views.VMainPage"
            Title="Timeline"
            BindingContext="{Binding MainPageViewModel, Source={StaticResource vmLocator}}">
    <ContentPage.Content>
        <Grid Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <controls:TimelineControl x:Name="tlc" Grid.Row="1"/>
            <Label Grid.Row="0" Text="{Binding Date.DateStr, ElementName=tlc}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
